Read-Me for Hadoop Example Java
=======================

To compile, you'll need the Hadoop core libraries (bundled with the Hadoop download). This includes, but may not be limited to, `hadoop-core-1.0.3.jar`.
